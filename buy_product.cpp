#include <iostream>
#include <string>
#include "buy_product.h"

/*Buy_product add(const Buy_product &lhs, const Buy_product &rhs)
{
  Buy_product sum = lhs;
  sum.combine(rhs);
  return sum;
}
std::istream &read(std::istream &is, Buy_product &prod)
{
  is >> prod.dayData >> prod.monthData >> prod.yearData 
    >> prod.articleProduct >> prod.nameProduct >> prod.cntProduct >> prod.priceBuyProduct;
  double sumBuyProduct = prod.cntProduct * prod.priceBuyProduct;
  return is;
}

std::ostream &print(std::ostream &os, const Buy_product &prod)
{
  os << prod.dayData << prod.monthData << prod.yearData 
    << prod.articleProduct << prod.nameProduct << prod.cntProduct << prod.priceBuyProduct;
  return os;
}*/
